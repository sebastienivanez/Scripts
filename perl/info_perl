Affichage :
print "\n"; --> saut de ligne
print "$Var"; --> affiche la valeur de Var

Boucles :
if(() and ())		--> si () et ()
if(/^mot/) 		--> si la ligne commence par mot
if(/^(mot)) 		--> tu peux faire un print $1; et il va afficher mot.
if(/^[a-zA-Z0-9]/)	--> si le mot commence par une lettre de "a"->"z" ou de "A"->"Z" ou de "0"->"9"
if(/^[+*]/)		--> si le mot commence par une lettre de "a"->"z" ou de "A"->"Z" ou de "0"->"9"(sauf "-" parce qu'il correspond a range et "/", ")"...)
if(/^[a-zA-Z0-9]_seb/)	--> tous les mots qui commencent par n'importe quelle lettre + chiffre et directement suivit par "_seb"
if(/^[a-zA-Z0-9]+_seb/)	--> tous les mots qui commencent par n'importe quelle lettre + chiffre et suivit par "_seb"
if(/^tm_\d/)		--> tous les mots qui commencent par "tm_" et qui contiennent un chiffre après

while(<INPFIL>) --> tant que le fichier n'est pas fini (parcourt ligne par ligne)

$Var=$ARGV[0] || die "ERROR: No Variable specified !"; --> stockage dans la variable Var le 1er argument qui suit la commande pour lancer le script perl 
open(INPFIL, $Var) || die "ERROR: Unable to read the file $Var"; --> ouverture du fichier Var
$Var=$Var."_out";	--> Var vaut $Var_out
