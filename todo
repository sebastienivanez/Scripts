Add a script to make git usage easier and quicker (13/12/2016)
	git start
		Does git status or git pull or create a git repository ...
		Then propose to do add
		Then if add propose to commit with message
		Then if commit propose to push 

Add a doxygen config and description on how to use it

Improve command "gr" to grep on a string
